language: go
sudo: false
go:
  - "1.9.x"
  - "1.10.x"
before_install:
  - go get github.com/golang/dep/...
install:
  - $GOPATH/bin/dep ensure -v
script:
  - ./coverage.sh
after_success:
  - bash <(curl -s https://codecov.io/bash)
